<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DOCTYPE -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 매퍼 설정 -->
<mapper namespace="web.dao.face.JoinDao">
	<select id="memberJoin" parameterType="web.dto.Login">
		INSERT INTO logintb (ID, PW, PHONE, EMAIL) VALUES (#{id}, #{pw}, #{phone}, #{email})
	</select>
</mapper>  
